dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "OmronDeviceSdk"
if (!System.env.JITPACK)
    include ':sample'
include ':omronsdk'
